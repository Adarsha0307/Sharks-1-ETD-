# Hypervisor Inspection Record

Status: NOT RUN

- Run ID:
- UTC time:
- Inspector:
- Hypervisor and version:
- Guest name/UUID:
- Guest OS/version:
- Source revision:
- Allocated vCPU/RAM/disk:
- Powered-off baseline snapshot UUID:

Mark each item PASS, FAIL, or BLOCKED and attach sanitized host-side evidence.

- All virtual NICs removed or disconnected:
- NAT and port forwarding absent:
- Bridged and host-only adapters absent:
- Shared folders absent:
- Clipboard disabled:
- Drag-and-drop disabled:
- Host disk mappings absent:
- USB/device passthrough disabled:
- Guest integration minimized:
- Temporary transfer media detached:
- Production credentials and real mailbox accounts absent:
- Guest IPv4 host/LAN/Internet probes blocked:
- Guest IPv6 host/LAN/Internet probes blocked:
- Guest external DNS blocked:
- Intended guest-local Docker traffic succeeds:
- Only guest loopback port 8080 published:
- Actual containers match non-root/restriction/resource expectations:
- Offline cold start used no pull/build/download:
- Baseline snapshot restore marker check passes:

Decision: NOT RUN
