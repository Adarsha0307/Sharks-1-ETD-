# Sandbox Verification Ledger

Phase b status: BLOCKED

No sandbox command has been run. The current Windows host session has no usable
Hyper-V tooling, WSL, Docker, VirtualBox, VMware CLI, QEMU, or Multipass, and
optional-feature inspection requires elevation. Source preparation is allowed;
application tests and fixture processing are NOT RUN.

Populate `sandbox/reports/runtime/` only from the isolated Ubuntu test VM after
host-side hypervisor inspection passes.
